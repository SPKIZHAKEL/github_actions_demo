name: 04 - Using Actions

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
        - name: Checkout code #want the workdlow to read code from the repo since react app is there
          uses: actions/checkout@v4
        - name: Printing folders
          run: ls